get:
  parameters:
    - name: id
      in: query
      required: true
  summary: Get article by id
  tags: [ Public ]
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: !schema Davajlama\Schemator\Demo\Schema\Response\Article